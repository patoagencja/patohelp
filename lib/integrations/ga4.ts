// GA4 Data API client — runs the dashboard reports (sessions by sourceMedium,
// by deviceCategory, top pages, new vs returning users, engagementRate) and
// normalizes them into `ga4_daily`. Property ID comes from
// `integrations.account_ids`.
// Implementation lands in Phase 2.

export {};
