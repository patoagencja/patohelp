// Google Ads API client — pulls campaign metrics from DRE's three customer
// accounts (dre, dre 2024, dre 2025) and normalizes them into `ads_daily`.
// Uses the shared developer token + OAuth client from env; customer IDs come
// from `integrations.account_ids`.
// Implementation lands in Phase 2.

export {};
