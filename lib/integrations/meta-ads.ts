// Meta Marketing API client — fetches campaign insights and normalizes them
// into `ads_daily` rows. Uses the shared META_APP_ID / META_APP_SECRET and a
// per-client user access token stored (encrypted) in `integrations`.
// Implementation lands in Phase 2.

export {};
